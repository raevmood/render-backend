version: "3.9"

services:
  assistant-api:
    build: .
    container_name: directed-assistant
    ports:
      - "8000:8000"
    volumes:
      - ./data/raw:/app/data/raw
      - ./data/chroma:/app/data/chroma
    restart: unless-stopped
